
{
  "no_background_content_button": 
  {
    "$default_text_color|default": "$title_text_color"  
    //Changes the Back button in the chat screen to have text that matches the title colour
  },

  "light_content_button": 
  {
    "$default_text_color": "$light_button_default_text_color" 
    //Fix for grey text on some buttons like the Store Filter options
  },

  "light_text_button": 
  {
    "$default_text_color": "$light_button_default_text_color" 
    //Fix for grey text on some buttons like the Store Filter options
  }
}
